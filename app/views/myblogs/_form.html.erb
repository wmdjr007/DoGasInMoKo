<%= form_with(model: myblog, local: true) do |form| %>
  <% if myblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(myblog.errors.count, "error") %> prohibited this myblog from being saved:</h2>

      <ul>
      <% myblog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :myblog_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :myblog_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
